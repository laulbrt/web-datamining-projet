# Web Crawling & NLP (TD1 — already done)
httpx>=0.27.0
trafilatura>=1.12.0
spacy>=3.7.0
# After installing spacy: python -m spacy download en_core_web_trf

# Knowledge Graph (TD2)
rdflib>=7.0.0
SPARQLWrapper>=2.0.0
requests>=2.31.0

# Reasoning (TD5 Part 1)
owlready2>=0.46

# KGE (TD5 Part 2) — install one of:
pykeen>=1.10.0          # recommended (pip install pykeen)
torch>=2.0.0            # required by pykeen
# Lightweight fallback uses only numpy (already in pykeen deps)
numpy>=1.24.0
scikit-learn>=1.3.0     # for t-SNE analysis

# RAG (TD6)
# Ollama installed separately: https://ollama.com/download
# Then: ollama pull gemma:2b

# Data processing
pandas>=2.0.0

# Utilities
tqdm>=4.66.0
matplotlib>=3.7.0       # for t-SNE plots
